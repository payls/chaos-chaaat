exports.optimiseImages = async (event) => {
  console.info('TRANSLOADIT_OPTIMISE_IMAGES_START', event);
  console.info(JSON.stringify(event));
  console.info('TRANSLOADIT_OPTIMISE_IMAGES_END', event);
};
